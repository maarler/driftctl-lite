// Package compare provides delta analysis between two drift scans.
// It identifies which resources moved from drifted to in-sync (resolved),
// which are newly drifted, and which remain drifted across scans.
package compare
