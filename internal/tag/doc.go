// Package tag provides utilities for filtering drift results by resource
// attribute tags. Rules are expressed as "key" or "key=value" strings and
// can be composed to narrow drift reports to specific resource groups.
package tag
