// Package enrich provides metadata enrichment for drift results.
//
// Rules are defined per resource type and attach owner, environment,
// cost-center and arbitrary custom annotations to matching results,
// enabling richer downstream reporting and policy evaluation.
package enrich
